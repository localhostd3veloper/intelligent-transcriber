# Intelligent Transcriber
## Dependencies
1. Python - You can download python by [Python Downloads](https://www.python.org/downloads/)
2. Use `pip install -r requirements.txt` to install all the dependencies for the project
